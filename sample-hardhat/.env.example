# Environment Configuration Example
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to version control!

# Sepolia RPC URL (can use public endpoints - these are NOT sensitive)
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# Private Key (KEEP THIS SECRET - NEVER COMMIT TO VERSION CONTROL)
# Generate a new private key from a test wallet
# DO NOT use the same key across multiple projects
# DO NOT expose this key publicly
PRIVATE_KEY=your_private_key_here_without_0x_prefix
